[build]
builder = "NIXPACKS"
buildCommand = "python3 -m pip install --upgrade pip && pip install -r backend/requirements.txt"

[deploy]
startCommand = "python3 -m uvicorn backend.server:app --host 0.0.0.0 --port 8000"
